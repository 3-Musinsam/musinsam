spring:
  application:
    name: payment
  config:
    import:
      - classpath:config/port.yml
      - classpath:config/db.yml
  profiles:
    group:
      local: local
      container: container
      test: test
      prod: production
    active: ${SPRING_ACTIVE_PROFILE:local}

